{
  "name": "@gateforge/shared",
  "version": "0.0.1",
  "description": "Shared DTOs, Types, and Utilities for GateForge Monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
